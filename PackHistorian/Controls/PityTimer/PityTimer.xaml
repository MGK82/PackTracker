<Window x:Class="PackTracker.Controls.PityTimer.PityTimer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PackTracker.Controls.PityTimer"
        mc:Ignorable="d"
        Title="PityTimer"
        Height="300"
        Width="300"
>
    <Window.Resources>
        <ResourceDictionary Source="..\Brushes.xaml" />
    </Window.Resources>
    <DockPanel>
        <local:BarChartCurr DockPanel.Dock="Right" x:Name="Ep_Curr" Fill="{StaticResource Epic}" Threshold="9" YTitle="Current Without Epic" />
        <local:BarChartPrev DockPanel.Dock="Left" x:Name="Ep_Prev" Fill="{StaticResource Epic}" Threshold="9" XTitle="Epic Packs" YTitle="Non Epic Packs" />
    </DockPanel>
</Window>
